from setuptools import setup, find_packages

setup(name='cardutil',
      version='0.1.0',
      author='Banktek Systems',
      author_email='admin@bankteksystems.com.au',
      packages=find_packages(),
      zip_safe=False)
